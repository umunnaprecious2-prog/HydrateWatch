# HyrateWatch Session Handoff

**Purpose:** Track session state between Claude conversations to ensure continuity and prevent lost context.

---

## Current Status

**Last Updated:** 2026-08-18
**Session State:** Backend verified end-to-end against the real CockroachDB cluster (locally and via Docker). Not yet deployed to Render - that's the user's turn to click through the dashboard.
**Branch:** main

---

## Last Completed Work

- Removed the legacy Python/FastAPI/SQLAlchemy backend (`backend/app`,
  `alembic/`, `requirements.txt`, venvs) - it's fully superseded now.
- Backend is TypeScript + Express + Prisma, running against **CockroachDB**.
  Prisma's `datasource` uses `provider = "cockroachdb"` - this is
  required, not a style choice: confirmed directly against a live
  cluster that Prisma's schema engine hard-refuses `migrate deploy` /
  `db push` with `provider = "postgresql"` even though CockroachDB is
  otherwise Postgres-wire-compatible. IDs use `@default(sequence())`
  (CockroachDB only allows `autoincrement()` on `BigInt`); migration SQL
  uses CockroachDB's dialect (`STRING`/`INT4`/`GENERATED BY DEFAULT AS
  IDENTITY`), generated offline via `prisma migrate diff --from-empty`.
- **Fully verified against the user's real CockroachDB cluster**
  (`hydratewatch-db`, AWS us-west-2): migration applied cleanly, all 6
  tables + `_prisma_migrations` exist, `npm run dev` connected and
  served `/api/v1/health` as 200, a real `/api/v1/auth/register` call
  round-tripped a user into the database (then cleaned up), and the
  Docker image (the exact one Render will run) did the same via
  `docker compose up backend`. Two real issues found and fixed along
  the way:
  - `sslmode=verify-full` needs a local root CA bundle file most
    machines don't have configured for Postgres tooling - switched to
    `sslmode=require` (still encrypted, skips CA-chain verification).
  - CockroachDB Cloud auto-locks a table's schema right after creation
    (changefeed optimization); a migration that creates a table then
    later indexes/references it in the same migration needs explicit
    `ALTER TABLE ... SET (schema_locked = false);` right after each
    `CREATE TABLE`, which the current migration file has.
  - Also added `connect_timeout=30&pool_timeout=30` to the connection
    string - Prisma's default 10s pool timeout was intermittently too
    tight for this network path (saw both fast successes and P1001
    timeouts on identical connection attempts).
- Wired up ESLint + Prettier for the backend (`npm run lint` / `format`)
  and fixed the lint errors that surfaced.
- Dockerized backend + frontend: `backend/Dockerfile`,
  `frontend/Dockerfile`, root `docker-compose.yml` (backend + frontend
  only now - no bundled local db service; point `DATABASE_URL` at a
  real CockroachDB cluster, Cloud Serverless free tier works for local
  dev too). Fixed three Docker bugs along the way: Prisma's telemetry
  hanging with no outbound network, Prisma's engine needing OpenSSL on
  `node:20-alpine`, and Next's standalone server binding to Docker's
  auto-set `HOSTNAME` instead of all interfaces.
- Added `render.yaml` (Render Blueprint: Docker backend + Node
  frontend). Field names are correct as of when this was written but
  unverified against Render's live parser (no Render API key available
  this session) - if the Blueprint import fails on a field, the
  dashboard's manual "New Web Service" flow is the fallback (documented
  in README.md).
- Added `FRONTEND_URL` env var (backend CORS) and tightened CORS to
  reject unknown origins in production (it previously allowed any
  origin unconditionally, even with `credentials: true`).
- Restored `claude.md` to the full HydrateWatch operating rules (a
  stray generic template had overwritten it); removed the leftover
  `claudee.md`, `prep.md`, `instruction.md` scaffolding.
- Shipped a small frontend fix: sensor data now refetches immediately
  after a successful upload instead of waiting for the next poll.

---

## Next Steps / Pending Tasks

- [ ] User: Render dashboard -> New -> Blueprint -> connect this repo,
      fill in the prompted env vars (DATABASE_URL is the same verified
      connection string, just with `sslmode=require&connect_timeout=30&pool_timeout=30`),
      deploy, then cross-wire `FRONTEND_URL` / `NEXT_PUBLIC_API_URL`
      once both services have a live URL (see README.md "Deploy to
      production").
- [ ] Point real Google OAuth credentials at `backend/.env` / root
      `.env` if Google sign-in needs to work locally (placeholders
      only right now).
- [ ] Consider adding a CI workflow (lint + build) now that ESLint/tsc
      are wired up.

---

## Known Issues

| Issue | Status | Notes |
|-------|--------|-------|
| Intermittent P1001 on individual connection attempts to the CockroachDB cluster | Mitigated | Longer `connect_timeout`/`pool_timeout` in DATABASE_URL papers over it; root cause looked like network latency from this sandboxed dev environment specifically, not the cluster. Retried, mostly fine. |
| render.yaml unverified against Render's live Blueprint parser | Open | No Render API key this session. Manual dashboard flow is the fallback if import fails. |

---

## Environment State

### Docker Compose
Needs `DATABASE_URL` in root `.env` pointed at a real CockroachDB cluster (already set, working - see `.env`, gitignored).

```powershell
docker compose up -d --build
```

- Backend: http://localhost:8000 (health: `/api/v1/health`)
- Frontend: http://localhost:3000

### Running without Docker
```powershell
cd backend
npm install
npm run prisma:generate
npm run prisma:migrate   # DATABASE_URL already points at the real cluster in backend/.env
npm run dev              # http://localhost:8000

cd frontend
npm install
npm run dev               # http://localhost:3000
```

### Production
Not yet deployed. `render.yaml` + README.md "Deploy to production" cover the steps; needs the user to click through the Render dashboard (account-level actions Claude can't do on their behalf). The database side is already proven working - same connection string, just pasted into Render's env var instead of a local `.env`.

---

## Important Notes for Next Session

1. Always read this file at session start
2. Update this file before ending a session
3. Check Git status before making changes
4. The backend is TypeScript/Express/Prisma/**CockroachDB** now (provider
   = "cockroachdb", not plain PostgreSQL) - and not the old Python backend.
5. `backend/.env` and root `.env` already have a real, working
   CockroachDB connection string (gitignored, not in git history). If
   picking this up again: check whether the user has deployed to Render
   yet (ask, or check for a live Render URL) before assuming the "Next
   Steps" above are still pending.

---

## Quick Reference

### Docker (whole stack):
```powershell
docker compose up -d --build
docker compose logs -f backend
docker compose down
```

### Backend (local):
```powershell
cd backend
npm run dev
```

### Frontend (local):
```powershell
cd frontend
npm run dev
```

### Check Git Status:
```powershell
git status
git ls-tree -r main --name-only
```

---

*This file is referenced by claude.md Section 5*
